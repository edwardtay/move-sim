/**
 * PostCSS Configuration for MoveSim Web UI
 * Configures Tailwind CSS and Autoprefixer for production builds
 */

export default {
  plugins: {
    // Tailwind CSS for utility-first styling
    tailwindcss: {},

    // Autoprefixer for browser compatibility
    autoprefixer: {},
  },
};
